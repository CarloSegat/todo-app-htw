# Todo App

A Flutter todo app with routing, image picking, and backend API integration.
Assuming your system is setup (i.e. flutter installed, see previous lectures)

## Setup

```
flutter pub get
```

## Run the app
```
flutter run
```

## Project structure

`lib/main.dart` is the entry point
`backend/` contains the backend (API server). See `backend/` for setup.
